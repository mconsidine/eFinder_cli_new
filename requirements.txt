# eFinder Python dependencies installed via pip wheels.
#
# This file does NOT include cedar-solve, which install.sh handles
# separately because it needs --no-deps + --no-build-isolation flags
# that don't fit into a requirements.txt.
#
# It also does NOT include the heavy compiled libraries
# (numpy, scipy, Pillow, flask, picamera2) -- those come from apt
# (python3-numpy, python3-scipy, python3-pil, python3-flask,
# python3-picamera2) and are visible to the venv via
# --system-site-packages.

grpcio
grpcio-tools
protobuf
