# eFinder Python dependencies.
#
# numpy and picamera2 come from apt (python3-numpy, python3-picamera2)
# via the venv's --system-site-packages flag, so they're not strictly
# required here. Listed as a backstop for non-system-site-packages
# installs; pip will see they're already satisfied and skip.
numpy

# Plate solver. cedar-solve's PyPI release (0.5.1, June 2024) is older
# than the GitHub HEAD; we use the API surface from HEAD (status codes,
# target_pixel + target_sky_coord parameters, RA_target/Dec_target keys).
# Install directly from GitHub.
#
# Pin to a specific commit hash for reproducibility -- bump the hash
# when you've validated against a newer cedar-solve revision. The
# package still installs as the `tetra3` module after install.
git+https://github.com/smroid/cedar-solve.git@master#egg=cedar-solve

# gRPC client for cedar-detect. Versions on PyPI for aarch64 wheels:
# grpcio has aarch64 manylinux wheels from 1.34+, recent releases work.
# grpcio-tools is needed only at install time (proto compilation).
grpcio
grpcio-tools
protobuf

# Web UI
flask
